name: frutty
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
    crypto: ^3.0.2
    flutter_local_notifications: ^12.0.4
    hive: ^2.2.3
    hive_flutter: ^1.1.0
    sensors_plus: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
