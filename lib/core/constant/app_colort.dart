import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF4CAF50);        // Hijau utama
  static const Color primaryLight = Color(0xFFC8E6C9);   // Hijau muda
  static const Color primaryDark = Color(0xFF388E3C);    // Hijau gelap
  static const Color accent = Color(0xFFFFC107);         // Kuning aksen

  static const Color background = Color(0xFFF5F5F5);     // Warna latar belakang umum
  static const Color textPrimary = Color(0xFF212121);    // Warna teks utama gelap
  static const Color textSecondary = Color(0xFF757575);  // Warna teks sekunder

  static const Color error = Color(0xFFD32F2F);          // Warna untuk error/alert
}
